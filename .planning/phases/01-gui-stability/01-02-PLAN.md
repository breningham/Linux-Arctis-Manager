---
phase: 01-gui-stability
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/linux_arctis_manager/gui_gtk/eq_math.py
  - tests/gui_gtk/test_eq_math.py
  - src/linux_arctis_manager/gui_gtk/app.py
autonomous: true
requirements:
  - GUI-03
must_haves:
  truths:
    - "Hovering or selecting a band renders at most one highlight curve for that type."
  artifacts:
    - path: "src/linux_arctis_manager/gui_gtk/eq_math.py"
      provides: "Pure functions to compute overall EQ response and per-type curves"
      exports: ["compute_response", "compute_type_curve"]
    - path: "tests/gui_gtk/test_eq_math.py"
      provides: "Unit tests describing expected curve behavior (single highlight per type)"
  key_links:
    - from: "EQCanvas._draw"
      to: "eq_math.compute_type_curve"
      via: "Use pure function to compute a single curve for the active type"
      pattern: "from linux_arctis_manager\.gui_gtk\.eq_math import"
---

<objective>
Ensure that only one highlight line is drawn per EQ type by moving curve calculations to a pure, testable module and driving behavior with tests.

Purpose: Prevent duplicate highlight lines and make the EQ rendering math verifiable.
Output: Unit-tested eq_math module and updated canvas draw code using it.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<feature>
  <name>Single highlight per type with testable math</name>
  <files>src/linux_arctis_manager/gui_gtk/eq_math.py, tests/gui_gtk/test_eq_math.py</files>
  <behavior>
    - Given a 40-value parametric list with multiple bands of the same type, compute_type_curve(values, active_type, width) returns exactly one y-array of length width+1 representing the summed response for that type.
    - Given values with different types, compute_response(values, width) returns a single overall response y-array of length width+1.
    - Edge: Bands with type 0 (Disabled) must be ignored.
  </behavior>
  <implementation>
    - Create eq_math.py exporting:
      - compute_response(values: list[float], width: int) -> list[float]
      - compute_type_curve(values: list[float], active_type: int, width: int) -> list[float]
    - Port the existing math from EQCanvas._draw into these functions.
    - Update EQCanvas._draw to call compute_response for the main curve and compute_type_curve for the active type, removing the separate single-band highlight drawing to avoid duplicate lines.
  </implementation>
</feature>
