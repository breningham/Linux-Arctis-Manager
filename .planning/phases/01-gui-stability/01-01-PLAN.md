---
phase: 01-gui-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/linux_arctis_manager/gui_gtk/app.py
autonomous: true
requirements:
  - GUI-01
must_haves:
  truths:
    - "After changing a band's type, the node editor popover can be closed by clicking outside the canvas (not only via Escape)."
    - "Clicking on empty canvas closes the editor when no drag is in progress."
  artifacts:
    - path: "src/linux_arctis_manager/gui_gtk/app.py"
      provides: "EQCanvas interaction: popover lifecycle and event handling"
      contains: "class EQCanvas"
  key_links:
    - from: "EQCanvas._on_type_change"
      to: "Popover close behavior"
      via: "ensure popover remains interactive and outside-click closes it"
      pattern: "popover\.popdown\(|set_can_target\(true|false\)"
---

<objective>
Fix the EQ node editor popover getting stuck after changing the band type by ensuring outside clicks close the editor and the canvas remains responsive.

Purpose: Restore expected UX so editing a node never freezes or traps focus.
Output: Updated EQCanvas popover handling that reliably closes on outside click and on empty-canvas click.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/GTK ARCHITECTURE.md
@src/linux_arctis_manager/gui_gtk/app.py

<interfaces>
- EQCanvas uses Gtk.GestureDrag, Gtk.GestureClick, Gtk.Popover with autohide.
- Current code disables canvas target while popover is open via set_can_target(False), which can prevent click handlers from running to close the popover.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make popover reliably closable via outside click</name>
  <files>src/linux_arctis_manager/gui_gtk/app.py</files>
  <action>
  - In EQCanvas._show_popover, remove the temporary input grab on the canvas (do not call self.set_can_target(False)). This allows canvas click gestures to receive events while the popover is open so clicking empty space can close it.
  - Ensure self.popover.set_autohide(True) is set (already present) and keep parent set to the canvas.
  - In EQCanvas._on_drag_end: when treating end as click (no drag threshold), show the popover as today; otherwise unchanged.
  - In EQCanvas click gesture handlers (pressed/released), keep logic that closes the popover when clicking off a node; with can_target not disabled, this now works while the popover is up.
  - Defensive: connect popover "closed" signal once to clear hover/selected state if needed and to ensure any temporary state is reset (selected_band may remain; keep as-is unless causing regressions).
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <done>
  - Clicking outside the node after changing Type in the editor closes the popover.
  - No UI lock or unresponsive state when the editor is open.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close editor on explicit Type change commit (safety net)</name>
  <files>src/linux_arctis_manager/gui_gtk/app.py</files>
  <action>
  - In EQCanvas._on_type_change, after updating self.values and notifying dbus_client, call self.popover.popdown() to dismiss the editor. This mirrors typical HIG behavior of committing the change and exiting, and it guarantees no stuck state even if autohide fails.
  - Do not disable canvas targeting anywhere in this flow.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <done>
  - Selecting a new Type immediately applies and closes the editor.
  - Subsequent clicks on nodes continue to open/close editors normally.
  </done>
</task>

</tasks>

<verification>
- Run pytest -q. Existing tests pass and no regressions introduced.
- Manual spot check (post-automation by human, optional): launch GUI and verify editor closes on outside click and after Type change.
</verification>

<success_criteria>
- No reproduction of the stuck popover bug after Type change.
- Canvas remains responsive to clicks and drags with the editor open and after it closes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gui-stability/01-01-SUMMARY.md`
</output>
