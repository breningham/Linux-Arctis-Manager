---
phase: 01-gui-stability
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/linux_arctis_manager/gui_gtk/app.py
  - tests/gui_gtk/test_eq_state_cache.py
autonomous: true
requirements:
  - GUI-02
must_haves:
  truths:
    - "Editing EQ in one target (e.g., Bluetooth Headphones) does not change cached or displayed values for other targets (2.4GHz Headphones, 2.4GHz Mic)."
  artifacts:
    - path: "tests/gui_gtk/test_eq_state_cache.py"
      provides: "Unit tests to ensure EqStateCache isolates state per target key"
  key_links:
    - from: "ArctisManagerWindow._cache_key_for_mode"
      to: "EqStateCache"
      via: "Consistent target keys (wi_hp, bt_hp, wi_mic, bt_mic) used for per-mode state separation"
      pattern: "wi_hp|bt_hp|wi_mic|bt_mic"
---

<objective>
Verify and enforce isolation of EQ state across the three targets using unit tests for EqStateCache and minor GUI safeguards.

Purpose: Prevent cross-contamination of EQ values/presets between modes.
Output: Tests proving per-target isolation and small GUI tweak to safely ignore updates when mode doesn't match.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EqStateCache isolation tests</name>
  <files>tests/gui_gtk/test_eq_state_cache.py</files>
  <behavior>
    - Setting wi_hp value and bt_hp value for the same setting_name yields distinct cached values.
    - set_preset/get_preset are isolated across keys.
    - Cache persists structure without raising when save/load roundtrips.
  </behavior>
  <action>
  - Create tests that instantiate EqStateCache with a temp SETTINGS_FOLDER (monkeypatch linux_arctis_manager.constants.SETTINGS_FOLDER to tmp_path) to avoid writing to user config.
  - Assert that values/presets for wi_hp and bt_hp do not overlap, and that wi_mic is independent as well.
  </action>
  <verify>
    <automated>pytest -q tests/gui_gtk/test_eq_state_cache.py</automated>
  </verify>
  <done>
  - Tests pass, proving isolation for values and presets across target keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard canvas updates by active mode (safety)</name>
  <files>src/linux_arctis_manager/gui_gtk/app.py</files>
  <action>
  - In _update_or_create_parametric_equalizer (existing branch that updates an already-created widget), retain the check to only update canvas values when mode == self._eq_mode. Strengthen it by also checking the ViewStack visible child name to avoid updating a hidden tab's canvas.
  - Add a short comment noting that this prevents visual bleed when multiple mode canvases exist.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <done>
  - Hidden canvases are not updated when switching modes; only the active mode's canvas reflects changes.
  </done>
</task>

</tasks>

<verification>
- Unit tests specifically for EqStateCache pass.
- General test suite passes, indicating no regressions in GUI helpers.
</verification>

<success_criteria>
- Distinct EQ state across wireless headphones, Bluetooth headphones, and microphone targets.
- No observed cross-bleed in cached values or preset selections between modes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gui-stability/01-03-SUMMARY.md`
</output>
