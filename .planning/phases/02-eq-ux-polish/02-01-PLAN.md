---
phase: 02-eq-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/linux_arctis_manager/gui_gtk/app.py
  - src/linux_arctis_manager/gui_gtk/preset_manager.py
  - tests/gui_gtk/test_preset_naming.py
autonomous: true
requirements: [GUI-04, GUI-05]
must_haves:
  truths:
    - "After changing a band's Type, clicking outside the editor closes it reliably (no stuck popovers)."
    - "Preset list shows friendly names without the 'GG:' prefix."
    - "Selecting a preset applies the correct values; cache persists the original key per target."
    - "Delete button visibility remains correct: built-ins (including GG imports) are not deletable; customs are."
  artifacts:
    - path: "src/linux_arctis_manager/gui_gtk/app.py"
      provides: "EQCanvas popover lifecycle safety on Type change"
      contains: "def _on_type_change"
      pattern: "self\\.popover\\.popdown\\(\)"
    - path: "src/linux_arctis_manager/gui_gtk/preset_manager.py"
      provides: "Friendly preset display naming helper"
      contains: "def friendly_preset_name("
    - path: "tests/gui_gtk/test_preset_naming.py"
      provides: "Unit tests for friendly preset naming and non-regression of builtin detection"
      min_lines: 20
  key_links:
    - from: "src/linux_arctis_manager/gui_gtk/app.py"
      to: "src/linux_arctis_manager/gui_gtk/preset_manager.py"
      via: "import friendly_preset_name to render display labels"
      pattern: "from linux_arctis_manager\\.gui_gtk\\.preset_manager import friendly_preset_name"
    - from: "UI preset ComboRow model (display names)"
      to: "Underlying preset keys (original names)"
      via: "parallel real_names list for selection mapping"
      pattern: "eq_widgets\\[\"real_names\"\\] = .*\[.*\]"
---

<objective>
Refine EQ UX so the editor popover always dismisses reliably after Type changes and the preset list shows friendly names (without the "GG:" prefix) while preserving correct behavior for selection, deletion, and caching.

Purpose: Remove lingering interaction friction and debug artifacts from the UI without altering underlying data semantics.
Output: Updated app.py and preset_manager.py with tests verifying naming behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gui-stability/01-01-SUMMARY.md
@.planning/phases/01-gui-stability/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure EQ editor popover closes after Type change (per GUI-04)</name>
  <files>src/linux_arctis_manager/gui_gtk/app.py</files>
  <action>
  - In class EQCanvas._on_type_change, after updating self.values and notifying dbus_client, explicitly call self.popover.popdown() to guarantee the editor closes immediately after a Type change. This addresses reports of the popover staying open after changing EQ Type.
  - Keep autohide and outside-click gestures intact; do not remove existing outside-click handlers.
  - Rationale: Safety-net explicit popdown avoids any event-ordering edge cases where autohide might be ignored after internal widget changes.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('src/linux_arctis_manager/gui_gtk/app.py').read_text()
sec = p.find('def _on_type_change')
assert sec!=-1, 'missing _on_type_change'
snippet = p[sec:sec+600]
assert 'popover.popdown()' in snippet, 'expected explicit popdown in _on_type_change'
print('OK')
PY
    </automated>
  </verify>
  <done>
  - _on_type_change contains an explicit self.popover.popdown() call
  - Outside click continues to dismiss the popover (no changes regressed)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Show friendly preset names without "GG:" while mapping to real keys (per GUI-05)</name>
  <files>src/linux_arctis_manager/gui_gtk/preset_manager.py, src/linux_arctis_manager/gui_gtk/app.py, tests/gui_gtk/test_preset_naming.py</files>
  <behavior>
    - friendly_preset_name('GG: Arc Raiders') -> 'Arc Raiders'
    - friendly_preset_name('Flat') -> 'Flat'
    - is_builtin_parametric('GG: Arc Raiders', 'output') remains True (non-deletable)
  </behavior>
  <action>
  - In preset_manager.py, add helper:
    def friendly_preset_name(name: str) -> str:
        return name[4:].strip() if isinstance(name, str) and name.startswith('GG: ') else name
  - In app.py where parametric preset ComboRow models are built/updated (in _update_or_create_parametric_equalizer, on_stack_change, and _update_routing paths):
    - Build two parallel lists: real_names (original keys) and disp_names = [friendly_preset_name(n) for n in real_names].
    - Use disp_names for Gtk.StringList model; store both lists on eq_widgets (e.g., eq_widgets["real_names"], eq_widgets["preset_names"] now represents display names).
    - When reading current selection or setting selection, map via index to real_names for value lookup, delete visibility, cache set_preset, and is_builtin checks. Display continues to show disp_names.
    - Ensure "Custom" remains as a display-only option at the end when there is no matching preset; it should not appear in real_names and should not be saved to cache as a preset key.
  - Add tests in tests/gui_gtk/test_preset_naming.py to validate friendly_preset_name behavior and non-regression of is_builtin_parametric for GG-imported names.
  </action>
  <verify>
    <automated>pytest -q tests/gui_gtk/test_preset_naming.py -q</automated>
  </verify>
  <done>
  - Preset dropdowns render names without the "GG:" prefix
  - Selecting a displayed preset applies the values of the correct underlying key
  - Cache persists the original key; delete button visibility logic unchanged for built-ins vs custom
  - Tests in test_preset_naming.py pass
  </done>
</task>

</tasks>

<verification>
- Popover change is validated by a static check asserting self.popover.popdown() exists inside _on_type_change
- Naming behavior validated by pytest passing tests in tests/gui_gtk/test_preset_naming.py
</verification>

<success_criteria>
- GUI-04 satisfied: Users never observe a stuck editor after changing Type; outside click reliably dismisses the editor
- GUI-05 satisfied: No preset name shows a "GG:" prefix in the UI; selecting/deleting/caching still maps correctly to the original keys
</success_criteria>

<output>
After completion, create `.planning/phases/02-eq-ux-polish/02-01-SUMMARY.md`
</output>
