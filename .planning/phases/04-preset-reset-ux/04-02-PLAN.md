---
phase: 04-preset-reset-ux
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/linux_arctis_manager/gui_gtk/app.py
autonomous: true
requirements: [PR-03, PR-04]
must_haves:
  truths:
    - "Reset action appears inside the revealer only when the selected preset has been modified (per PR-02)"
    - "Clicking Reset restores values to the selected preset (per PR-01)"
    - "Reset button uses destructive styling and sits alongside 'Save Preset' in the revealer (per PR-03)"
    - "Behavior implemented for both Parametric and Graphic EQ flows (per PR-04)"
  artifacts:
    - path: "src/linux_arctis_manager/gui_gtk/app.py"
      provides: "UI wiring: reset-in-revealer for parametric and graphic EQ"
      contains: "from linux_arctis_manager.gui_gtk.eq_ui_logic import should_show_reset, get_reset_target"
  key_links:
    - from: "src/linux_arctis_manager/gui_gtk/app.py"
      to: "src/linux_arctis_manager/gui_gtk/eq_ui_logic.py"
      via: "imports: should_show_reset/get_reset_target"
      pattern: "eq_ui_logic"
---

<objective>
Wire the new Reset behavior into the GTK UI: place a destructive "Reset to Preset" button inside the existing revealer, show it only when the current values differ from the selected preset, and apply this to both Parametric and Graphic EQ editors.

Purpose: Honor UX decision to reset to the chosen preset (not Flat) and reduce clutter by surfacing the action only when relevant, consistent with GNOME HIG patterns.
Output: Modified app.py with reset-in-revealer for Parametric and Graphic flows using pure logic helpers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/linux_arctis_manager/gui_gtk/app.py

User decisions (locked):
- D-01: Reset should restore the selected preset, not Flat.
- D-02: Show Reset only when a change was made to a preset.
- D-03: Put Reset in the revealer and style as destructive.

Interfaces to use:
From src/linux_arctis_manager/gui_gtk/eq_ui_logic.py (Plan 04-01):
```python
def should_show_reset(selected: str | None, current: list[float], presets: dict[str, list[float]], mode: str) -> bool
def get_reset_target(selected: str | None, presets: dict[str, list[float]]) -> list[float] | None
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parametric EQ — add Reset-in-revealer (per D-01, D-02, D-03)</name>
  <files>src/linux_arctis_manager/gui_gtk/app.py</files>
  <action>
    - Import helpers: `from linux_arctis_manager.gui_gtk.eq_ui_logic import should_show_reset, get_reset_target` at module top.
    - In _update_or_create_parametric_equalizer:
      - Remove the existing Reset row that creates a separate Adw.ActionRow with a "Reset to Flat" button.
      - In the revealer's box (rbox) that currently holds the name Entry and "Save Preset" button, add a new Gtk.Button label "Reset to Preset" before the Save button.
      - Style it with `destructive-action` (per GNOME HIG) and `pill` classes; do NOT use `suggested-action`.
      - Determine selected real preset name using eq_widgets["real_names"][preset_row.get_selected()] when index in range; otherwise treat as None/"Custom".
      - Update the existing `_update_revealer_visibility` method to also toggle the Reset button visibility using `should_show_reset(selected_real, current_values, presets, mode="parametric")`.
      - Connect Reset button clicked to:
        - Compute `target = get_reset_target(selected_real, presets)`; if present, set canvas values to target, update last_value, call `dbus_client.change_setting`, persist _eq_cache value and preset (selected_real), and hide the revealer.
      - Ensure the revealer still opens when current values don't match any preset (existing behavior) and that on preset selection the revealer is hidden as before.
    - Notes:
      - Using eq_ui_logic per D-01/D-02; using destructive styling per D-03.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <done>
    - Separate Reset row removed for parametric; Reset button exists inside revealer and is destructive-styled.
    - Reset click restores selected preset values and hides revealer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Graphic EQ — add Reset-in-revealer (per D-01, D-02, D-03)</name>
  <files>src/linux_arctis_manager/gui_gtk/app.py</files>
  <action>
    - In _update_or_create_equalizer (graphic):
      - Remove the existing Reset row with "Reset to Flat".
      - In the revealer's rb box that holds the name Entry and "Save Preset" button, add a Gtk.Button label "Reset to Preset" before Save.
      - Style with `destructive-action` and `pill` CSS classes.
      - Compute selected preset name from eq_widgets["preset_names"][preset_row.get_selected()] and validate against current graphic presets dict.
      - Add a helper to compute current values from scales: `[float(sc.get_value()) for sc in eq_widgets["scales"]]` and toggle Reset visibility using `should_show_reset(selected_real, current, presets, mode="graphic")` within `_update_revealer_visibility_from_scales` and on selection changes.
      - On click, set all scale values to the target list from `get_reset_target(selected_real, presets)`, update last_value, call `dbus_client.change_setting`, and hide the revealer.
    - Preserve existing behavior: revealer opens when current values don't match any preset and hides on explicit preset selection.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <done>
    - Separate Reset row removed for graphic; Reset button exists inside revealer and is destructive-styled.
    - Reset click restores selected preset values and hides revealer.
  </done>
</task>

</tasks>

<verification>
- Full pytest suite passes; import of app.py succeeds and logic tests (Plan 04-01) continue to pass.
</verification>

<success_criteria>
- Reset-in-revealer implemented for Parametric and Graphic flows.
- Reset button visible only when current values differ from the selected preset.
- Clicking Reset restores to the selected preset; no Flat reset remains.
</success_criteria>

<output>
After completion, create `.planning/phases/04-preset-reset-ux/04-02-SUMMARY.md`
</output>
