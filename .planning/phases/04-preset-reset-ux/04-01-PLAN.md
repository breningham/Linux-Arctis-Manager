---
phase: 04-preset-reset-ux
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/linux_arctis_manager/gui_gtk/eq_ui_logic.py
  - tests/gui_gtk/test_eq_ui_logic.py
autonomous: true
requirements: [PR-01, PR-02]
must_haves:
  truths:
    - "When a real preset is selected and bands are modified, logic reports reset is available (per PR-02)"
    - "Reset target equals the selected preset's original values, not Flat (per PR-01)"
  artifacts:
    - path: "src/linux_arctis_manager/gui_gtk/eq_ui_logic.py"
      provides: "Pure functions to decide reset visibility and target"
      exports: ["should_show_reset", "get_reset_target"]
    - path: "tests/gui_gtk/test_eq_ui_logic.py"
      provides: "Unit tests for visibility/target logic"
      contains: "class TestEqUiLogic"
  key_links:
    - from: "tests/gui_gtk/test_eq_ui_logic.py"
      to: "src/linux_arctis_manager/gui_gtk/eq_ui_logic.py"
      via: "import functions and assert behavior"
      pattern: "from linux_arctis_manager.gui_gtk.eq_ui_logic import"
---

<objective>
Introduce pure, testable logic for EQ Reset behavior so UI can reliably show a destructive Reset to Preset action only when the current values differ from the selected preset, and compute the correct target values to restore.

Purpose: Separate decision logic from GTK wiring to enable robust unit testing and prevent UI regressions.
Output: New logic module with passing tests for both Parametric (values_match) and Graphic (exact) comparisons.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

User decisions (locked):
- D-01: Reset should restore the selected preset, not Flat.
- D-02: Show Reset only when a change was made to a preset (not when on "Custom").

Interfaces from existing code:
From src/linux_arctis_manager/gui_gtk/preset_manager.py:
```python
def values_match(a: list[float] | dict, b: list[float] | dict, tol: float = 0.05) -> bool
PARAMETRIC_EQ_PRESETS: dict[str, list[float]]
def friendly_preset_name(name: str) -> str
```
</context>

<feature>
  <name>Pure reset visibility/target logic</name>
  <files>src/linux_arctis_manager/gui_gtk/eq_ui_logic.py, tests/gui_gtk/test_eq_ui_logic.py</files>
  <behavior>
    - Graphic EQ: should_show_reset(selected="Smiley", current=[..], presets) is True iff current != presets["Smiley"].
    - Graphic EQ: get_reset_target("Smiley", presets) returns presets["Smiley"].
    - Parametric EQ: should_show_reset(selected="Arc Raiders", current_40, presets, mode="parametric") is True iff not values_match(current_40, presets["Arc Raiders"]).
    - Parametric EQ: get_reset_target("Arc Raiders", presets) returns the 40-value list for that preset.
    - If selected is None or "Custom" or not present in presets → should_show_reset returns False and get_reset_target returns None.
  </behavior>
  <implementation>
    - Create eq_ui_logic.py exporting:
      - should_show_reset(selected: str | None, current: list[float], presets: dict[str, list[float]], mode: str) -> bool
        - mode in {"graphic", "parametric"}; for parametric, use preset_manager.values_match; for graphic, use exact list equality (cast to floats/ints consistently).
        - If selected is falsy, equals "Custom", or not a key in presets: return False.
      - get_reset_target(selected: str | None, presets: dict[str, list[float]]) -> list[float] | None
        - Return a shallow copy of presets[selected] if available; else None.
    - Write tests under tests/gui_gtk/test_eq_ui_logic.py covering all behavior bullets with small fixtures.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Add failing tests for reset logic (per D-01, D-02)</name>
  <files>tests/gui_gtk/test_eq_ui_logic.py</files>
  <behavior>
    - Test graphic mode visibility True when list differs; False when equal.
    - Test parametric mode visibility uses values_match tolerance.
    - Test get_reset_target returns expected list or None for invalid/Custom.
  </behavior>
  <action>Create tests asserting the behavior above. Import the (to-be-created) functions from linux_arctis_manager.gui_gtk.eq_ui_logic. Do not implement the module yet.</action>
  <verify><automated>pytest -q tests/gui_gtk/test_eq_ui_logic.py -q || true</automated></verify>
  <done>Tests exist and fail because implementation is missing.</done>
</task>

<task type="auto">
  <name>Task 1: Implement eq_ui_logic to make tests pass (per D-01, D-02)</name>
  <files>src/linux_arctis_manager/gui_gtk/eq_ui_logic.py</files>
  <action>
    - Implement should_show_reset and get_reset_target exactly per Implementation.
    - Import values_match from preset_manager for parametric mode.
    - Ensure no GTK imports; pure functions only.
  </action>
  <verify><automated>pytest -q tests/gui_gtk/test_eq_ui_logic.py -q</automated></verify>
  <done>All tests in test_eq_ui_logic pass.</done>
</task>

</tasks>

<verification>
- pytest passes for test_eq_ui_logic
</verification>

<success_criteria>
- New logic module exists with exported functions.
- Tests cover graphic and parametric cases and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-preset-reset-ux/04-01-SUMMARY.md`
</output>
