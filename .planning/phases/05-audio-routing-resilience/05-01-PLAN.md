---
phase: 05-audio-routing-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/linux_arctis_manager/dbus_service.py
  - src/linux_arctis_manager/pactl.py
  - tests/daemon/test_pulse_device_options.py
  - tests/daemon/test_pactl_redirect.py
autonomous: true
requirements:
  - AR-01
  - AR-02
must_haves:
  truths:
    - "Settings dropdown lists actual PulseAudio sinks, not just 'none'"
    - "Choosing a saved device like 'Soundbar' successfully redirects default sink"
  artifacts:
    - path: "src/linux_arctis_manager/dbus_service.py"
      provides: "Robust PulseAudio device options for settings (id + label)"
      contains: "get_list_options"
    - path: "src/linux_arctis_manager/pactl.py"
      provides: "Resilient redirect matching by node.nick/name/description"
      contains: "redirect_audio"
    - path: "tests/daemon/test_pulse_device_options.py"
      provides: "Tests for options list population without node.nick"
    - path: "tests/daemon/test_pactl_redirect.py"
      provides: "Tests for redirect sink matching across properties"
  key_links:
    - from: "src/linux_arctis_manager/dbus_service.py"
      to: "GUI settings SELECT field"
      via: "D-Bus list options payload consumed by gui_gtk.app (_option_lists)"
      pattern: "pulse_audio_devices"
    - from: "src/linux_arctis_manager/pactl.py"
      to: "CoreEngine.redirect_audio_on_disconnect"
      via: "PulseAudioManager.redirect_audio called with saved device string"
      pattern: "redirect_audio\(.*\)"
---

<objective>
Fix PulseAudio device selection and redirection reliability.

Purpose: Ensure the settings dropdown shows real sinks and that selecting a device like "Soundbar" actually redirects audio on disconnect/connect events.
Output: Robust D-Bus options list and resilient redirect matching with tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/linux_arctis_manager/dbus_service.py
@src/linux_arctis_manager/pactl.py
@tests/daemon/test_dbus_settings.py
</context>

<tasks>

<task type="auto">
  <name>Implement robust PulseAudio device options (per AR-01)</name>
  <files>src/linux_arctis_manager/dbus_service.py, tests/daemon/test_pulse_device_options.py</files>
  <action>
  - Update ArctisManagerDbusSettingsService.get_list_options for list_name == "pulse_audio_devices" to build options even when node.nick is missing.
  - For each sink in pulse.sink_list():
    - id: prefer proplist['node.name'] (stable), else proplist['node.nick'], else proplist['device.string'] or sink.name
    - description (label): prefer proplist['node.nick'] or proplist['node.description'] or proplist['device.description'], else id
    - Append {"id": id, "name": description}
  - Keep existing JSON structure (list of dicts), GUI consumes 'name' for labels.
  - Add tests in tests/daemon/test_pulse_device_options.py to simulate pulsectl sink objects with varying proplists (missing node.nick) and assert non-empty options and correct id/label selection.
  - Notes: Do not change D-Bus method signature. Ensure function returns valid JSON string.
  </action>
  <verify>
    <automated>pytest -q tests/daemon/test_pulse_device_options.py -q</automated>
  </verify>
  <done>
    - get_list_options returns at least one option when PulseAudio has sinks
    - Options contain user-friendly names and stable ids
  </done>
</task>

<task type="auto">
  <name>Harden redirect_audio sink matching (per AR-02)</name>
  <files>src/linux_arctis_manager/pactl.py, tests/daemon/test_pactl_redirect.py</files>
  <action>
  - Add a helper in PulseAudioManager to find a sink by identifier with priority order:
    1) Exact match on node.name
    2) Exact match on node.nick
    3) Exact match on node.description
    4) Case-insensitive substring match on node.nick/node.description
  - Update redirect_audio to use the helper; log a warning if not found and include available candidates in the message for diagnostics.
  - Add tests in tests/daemon/test_pactl_redirect.py mocking sink_list_wrapper() to return fake sink entries with only description present and assert redirect_audio finds the sink.
  </action>
  <verify>
    <automated>pytest -q tests/daemon/test_pactl_redirect.py -q</automated>
  </verify>
  <done>
    - redirect_audio selects the intended sink when given a nickname or description
    - Logs no error when a matching sink exists
  </done>
</task>

</tasks>

<verification>
- Run full daemon-related tests: pytest -q tests/daemon -q should pass.
</verification>

<success_criteria>
- Dropdown shows real sinks on systems where node.nick is absent.
- Redirecting to a human-entered device like "Soundbar" succeeds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-audio-routing-resilience/05-01-SUMMARY.md`
</output>
