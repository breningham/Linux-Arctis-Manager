---
phase: 05-audio-routing-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/linux_arctis_manager/core.py
  - tests/core/test_core_parametric_alias.py
  - tests/gui_gtk/test_smoke_offline_hero.py
autonomous: true
requirements:
  - AR-03
  - AR-04
must_haves:
  truths:
    - "No 'Unknown setting: parametric_eq' warning in daemon logs when applying EQ"
    - "GTK app opens to a non-blank window and shows offline/disconnected hero state when no device is online"
  artifacts:
    - path: "src/linux_arctis_manager/core.py"
      provides: "Alias handling for 'parametric_eq' to the configured PARAMETRIC_EQ setting"
      contains: "on_setting_changed"
    - path: "tests/core/test_core_parametric_alias.py"
      provides: "Test exercising alias mapping with real-like config name 'equalizer'"
    - path: "tests/gui_gtk/test_smoke_offline_hero.py"
      provides: "GTK smoke test ensuring offline hero visibility (non-blank UI)"
  key_links:
    - from: "src/linux_arctis_manager/core.py"
      to: "src/linux_arctis_manager/config.py"
      via: "SettingType.PARAMETRIC_EQ matching irrespective of setting name"
      pattern: "SettingType\.PARAMETRIC_EQ"
    - from: "tests/gui_gtk/test_smoke_offline_hero.py"
      to: "src/linux_arctis_manager/gui_gtk/app.py"
      via: "ArctisManagerWindow construction and offline hero state update"
      pattern: "ArctisManagerWindow"
---

<objective>
Eliminate parametric EQ alias warnings and ensure GTK launches with a visible offline state.

Purpose: Map 'parametric_eq' setting updates to the device's configured PARAMETRIC_EQ field and confirm the UI is not blank by default.
Output: Alias-aware CoreEngine behavior and a GTK smoke test.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/linux_arctis_manager/core.py
@src/linux_arctis_manager/config.py
@src/linux_arctis_manager/gui_gtk/app.py
</context>

<tasks>

<task type="auto">
  <name>Map 'parametric_eq' alias to actual PARAMETRIC_EQ config (per AR-03)</name>
  <files>src/linux_arctis_manager/core.py, tests/core/test_core_parametric_alias.py</files>
  <action>
  - Modify CoreEngine.on_setting_changed:
    - Keep existing config lookup by name.
    - If no config is found and setting == 'parametric_eq', search all device_config.settings across sections for the first setting where type == SettingType.PARAMETRIC_EQ and use that config for subsequent logic (packing and send_command).
    - Preserve existing persistence to EqStore for banks.
  - Add tests in tests/core/test_core_parametric_alias.py:
    - Build a minimal StubConfig whose PARAMETRIC_EQ setting is named 'equalizer'.
    - Monkeypatch send_command/get_command_endpoint_address to no-op.
    - Call on_setting_changed('parametric_eq', vals_40) and assert no exception and that EqStore persisted values for current target; also assert alias resolution path used.
  </action>
  <verify>
    <automated>pytest -q tests/core/test_core_parametric_alias.py -q</automated>
  </verify>
  <done>
    - on_setting_changed handles 'parametric_eq' even when config uses 'equalizer'
    - Tests pass and no 'Unknown setting' log is produced during call
  </done>
</task>

<task type="auto">
  <name>GTK smoke: verify offline hero renders (per AR-04)</name>
  <files>tests/gui_gtk/test_smoke_offline_hero.py</files>
  <action>
  - Create a GTK test similar to tests/gui_gtk/test_eq_isolation_ui.py that:
    - Instantiates ArctisManagerWindow in an isolated HOME tmpdir and replaces dbus_client with a stub.
    - Calls on_status_received({}) to simulate 'no device detected' state.
    - Asserts that the hero widget is visible and has the expected title/description strings set (non-blank UI).
  - This guards against regressions where the window renders blank when offline.
  </action>
  <verify>
    <automated>pytest -q tests/gui_gtk/test_smoke_offline_hero.py -q</automated>
  </verify>
  <done>
    - GTK test passes, ensuring base window is not blank in offline conditions
  </done>
</task>

</tasks>

<verification>
- Run related test suites: pytest -q tests/core -q && pytest -q tests/gui_gtk/test_smoke_offline_hero.py -q
</verification>

<success_criteria>
- Daemon no longer logs 'Unknown setting: parametric_eq' when EQ updates apply
- GTK opens reliably to an offline/disconnected hero state when the device is not online
</success_criteria>

<output>
After completion, create `.planning/phases/05-audio-routing-resilience/05-02-SUMMARY.md`
</output>
