---
phase: 03-eq-isolation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/gui_gtk/test_eq_isolation_ui.py
autonomous: true
requirements:
  - EQISO-01
  - EQISO-02
  - EQISO-03
  - EQISO-04
must_haves:
  truths:
    - "Each EQ tab (Wireless, Bluetooth, Mic) has its own independent parametric EQ canvas and editing one does not affect the others."
    - "Switching EQ tabs updates the daemon eq_target accordingly (bluetooth, microphone, wireless)."
    - "Only the visible tab's canvas updates when settings change; hidden canvases are not mutated by updates."
    - "Preset lists show friendly names without the 'GG:' prefix while internal keys remain unchanged for logic/deletion."
  artifacts:
    - path: "tests/gui_gtk/test_eq_isolation_ui.py"
      provides: "Integration-style GTK tests verifying EQ isolation, tab target sync, and friendly preset display"
      contains: "test_parametric_canvases_isolated_across_tabs"
    - path: "src/linux_arctis_manager/gui_gtk/app.py"
      provides: "EQ UI logic (ViewStack tabs, D-Bus target switching, per-canvas updates)"
      contains: "_update_or_create_parametric_equalizer"
    - path: "src/linux_arctis_manager/gui_gtk/preset_manager.py"
      provides: "EqStateCache and preset helpers (friendly names, builtins)"
      contains: "class EqStateCache"
  key_links:
    - from: "src/linux_arctis_manager/gui_gtk/app.py"
      to: "GtkDbusClient.set_eq_target"
      via: "ViewStack visible-child change handler"
      pattern: "set_eq_target\(\"(wireless|bluetooth|microphone)\"\)"
    - from: "src/linux_arctis_manager/gui_gtk/app.py"
      to: "EQCanvas.set_values"
      via: "Only when mode == _eq_mode and visible tab matches"
      pattern: "w\[\"canvas\"\]\.set_values\("
---

<objective>
Harden EQ isolation and finalize UX by adding automated tests that prove three independent Parametric EQ canvases/controllers, verify D-Bus eq_target stays synchronized with the active tab, and ensure only the visible canvas updates. Also assert friendly preset display (no 'GG:' prefixes) while preserving internal key mapping.

Purpose: Prevent regressions in cross-tab isolation and tab-target synchronization; lock in friendly preset naming behavior across the GTK UI.
Output: A new pytest module covering isolation, target sync, and friendly naming behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/STRUCTURE.md
@src/linux_arctis_manager/gui_gtk/app.py
@src/linux_arctis_manager/gui_gtk/preset_manager.py
@tests/gui_gtk/test_eq_state_cache.py
@tests/gui_gtk/test_preset_naming.py
@tests/conftest.py

<interfaces>
From src/linux_arctis_manager/gui_gtk/app.py:

class ArctisManagerWindow(Adw.ApplicationWindow):
  def refresh_settings_ui(self): ...
  def _update_or_create_parametric_equalizer(self, name, value, cfg, group, mode: str = "wireless"): ...

Key behaviors:
- Responds to Adw.ViewStack visible-child change by calling dbus_client.set_eq_target("wireless"|"bluetooth"|"microphone").
- Updates only the currently active EQ target's canvas; guards against updating hidden canvases by checking visible tab name.

From src/linux_arctis_manager/gui_gtk/preset_manager.py:

class EqStateCache:  # per-target cache with keys wi_hp, bt_hp, wi_mic, bt_mic
  def get_value(self, target_key: str, setting_name: str) -> list[float] | None
  def set_value(self, target_key: str, setting_name: str, values: list[float]) -> None
  def get_preset(self, target_key: str, setting_name: str) -> str | None
  def set_preset(self, target_key: str, setting_name: str, preset_name: str) -> None

def friendly_preset_name(name: str) -> str  # strips leading 'GG: '
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration-style GTK tests for EQ isolation and tab→eq_target sync (per EQISO-01, EQISO-02, EQISO-03)</name>
  <files>tests/gui_gtk/test_eq_isolation_ui.py</files>
  <action>
  - Create tests that:
    - Instantiate ArctisManagerWindow with a stub D-Bus client and a fake ViewStack-like object exposing get/set_visible_child_name.
    - Build three parametric EQ widgets via _update_or_create_parametric_equalizer for modes wireless, bluetooth, microphone, each seeded with distinct values.
    - Assert hidden canvases do not update when mode != _eq_mode or when visible tab name does not match (only visible canvas updates).
    - Create the ViewStack by simulating settings with a parametric_eq entry, then programmatically change visible child to 'bluetooth' and 'microphone' and assert dbus_client.set_eq_target receives those modes.
  - Ensure tests isolate disk writes by monkeypatching linux_arctis_manager.constants.SETTINGS_FOLDER to tmp_path.
  - Use pytest.importorskip('gi') to skip gracefully if GTK libraries are unavailable.
  </action>
  <verify>
    <automated>pytest -q tests/gui_gtk/test_eq_isolation_ui.py -q -k "isolation or target"</automated>
  </verify>
  <done>
  - Tests pass, confirming only the visible canvas updates and D-Bus target switches match the active tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test to verify friendly preset names in UI (per EQISO-04)</name>
  <files>tests/gui_gtk/test_eq_isolation_ui.py</files>
  <action>
  - Extend the test module to inject a custom parametric preset with a 'GG: ' prefix via PresetManager.add_parametric_preset('GG: Test …', ..., mode='output').
  - Rebuild a wireless parametric EQ widget and assert the display model uses friendly_preset_name (no 'GG:' visible) while underlying real names remain intact for logic/deletion.
  </action>
  <verify>
    <automated>pytest -q tests/gui_gtk/test_eq_isolation_ui.py -q -k friendly</automated>
  </verify>
  <done>
  - UI preset display names contain no 'GG:' while functional mapping remains unchanged.
  </done>
</task>

</tasks>

<verification>
- End-to-end: pytest -q tests/gui_gtk/test_eq_isolation_ui.py passes locally.
- Regression: existing tests (eq_state_cache, preset_naming, eq_math) continue to pass with `pytest -q`.
</verification>

<success_criteria>
- All new tests pass and demonstrate isolation and tab-target synchronization.
- No regressions in existing GUI/preset tests.
- ROADMAP requirements EQISO-01..04 are satisfied by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-eq-isolation-hardening/03-01-SUMMARY.md`
</output>
